[run]
install = "true"
dev = "python3 -m http.server 8080 --bind 0.0.0.0"
build:dev = "rm -rf dist && mkdir -p dist/css dist/js && cp *.html dist/ && cp -r css/* dist/css/ && cp -r js/* dist/js/ && cp 'usa logo.png' dist/"
